import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Deliveries = () => {
    return (
        <SafeAreaView>
            <Text>Gas Deliveries</Text>
        </SafeAreaView>
    )
};

export default Deliveries;